[tool.ruff]
target-version = "py312"
line-length = 88
exclude = ["mcp_servers"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
